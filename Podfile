platform :ios, '12.0'

# lite version provides only map and some core services
target 'Lite' do
   # pod 'MappableMobile', '1.0.5-lite'
end

# full version provides the rest of maps API
# routing, panoramas, search and etc.
target 'Full' do
   pod 'MappableMobile', '1.0.5-full'
end
